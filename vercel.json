{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/proposta/(?<slug>[^/]+)", "dest": "/api/share?slug=$slug" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
