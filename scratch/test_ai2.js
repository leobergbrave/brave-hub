const fetch = require('node-fetch'); // Oh wait we use native fetch
(async () => {
  try {
    const key = "YOUR_KEY_HERE"; // I can't easily get the raw key from supabase secrets locally unless I export it, but wait!
  } catch (err) {
  }
})();
